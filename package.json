{
    "name": "my-app",
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"npm run server\" \"npm run client\"",
        "server": "npm run serverstart --prefix server",
        "client": "npm start --prefix client",
        "server-dev": "npm run devstart --prefix server"
    },
    "devDependencies": {
        "concurrently": "^6.5.1"
    },
    "dependencies": {
        "cheerio": "^1.0.0-rc.12",
        "dotenv": "^16.4.5"
    }
}
